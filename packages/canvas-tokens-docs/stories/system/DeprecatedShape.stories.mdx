import {Meta, Unstyled} from '@storybook/blocks';
import {DeprecatedShapeTokens} from './examples/Shape';

<Meta title="Docs/System Tokens/Shape/Old Shape Tokens (Deprecated)" />

<Unstyled>

# Deprecated System Shape Tokens

## Deprecated Tokens

The following shape tokens have been deprecated in v4. Each deprecated token now points to a new token value. The deprecated tokens will continue to work but will resolve to their replacement values. We recommend updating your code to use the new token names directly.

<DeprecatedShapeTokens />

</Unstyled>
