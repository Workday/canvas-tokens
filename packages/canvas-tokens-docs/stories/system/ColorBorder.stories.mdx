import {Meta, Unstyled} from '@storybook/blocks';

import {PageHeader} from '../../components/PageHeader';
import {BorderColors} from './examples/Color/Border';

<Meta title="Docs/System Tokens/Color/Border" />

<Unstyled>

<PageHeader
  title="Border Colors"
  description="Border colors define boundaries against a background and separate content areas."
/>

## Usage

```ts
// styles.ts
import {system} from '@workday/canvas-tokens-web';

const styles = {
  borderColor: `var(${system.color.border.container})`,
};
```

```css
/* styles.css */
.card {
  border-color: var(--cnvs-sys-color-border-container);
}
```

## Tokens

<BorderColors />

</Unstyled>
