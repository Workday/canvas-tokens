import {Meta, Unstyled} from '@storybook/blocks';

import {PageHeader} from '../../components/PageHeader';
import {BackgroundColors} from './examples/Color/Background';

<Meta title="Docs/System Tokens/Color/Background" />

<Unstyled>

<PageHeader
  title="Background Colors"
  description="Background colors create the foundation for all other elements. Use them for page backgrounds, card surfaces, and container fills. All content — text, buttons, forms, and icons — should sit on top of a background color."
/>

<BackgroundColors />

</Unstyled>
