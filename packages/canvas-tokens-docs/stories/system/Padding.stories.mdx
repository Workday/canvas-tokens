import {Meta, Unstyled} from '@storybook/blocks';
import {PaddingTokens} from './examples/Padding';

<Meta title="Docs/System Tokens/Padding" />

<Unstyled>

# System Padding Tokens



## Usage

Padding tokens can be consumed as a JavaScript / TypeScript object. Each token value references the
CSS variable from a base token, so you'll need to have those CSS variables imported in your
application as well.

```ts
// styles.ts
import {system} from '@workday/canvas-tokens-web';

const styles = {
  padding: `var(${system.padding.sm})`,
};
```

They can also be consumed as CSS, Sass, or Less variables. In the example below, we're applying the
same styles using CSS variables directly.

```css
// styles.css
.card {
  padding: var(--cnvs-sys-padding-sm);
}
```

---

## Tokens

These tokens use a multiplier scale and reference our `base.baseline` token, which is `0.5rem` / `8px`.
So the `system.padding.sm` token has a value calculated from the baseline multiplier.

<PaddingTokens />

</Unstyled>
