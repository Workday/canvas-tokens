import {Meta, Unstyled} from '@storybook/blocks';
import {SizeTokens, PaddingTokens, GapTokens, DeprecatedSpaceTokens} from './examples/Space';

<Meta title="Docs/System Tokens/Space/Old Space Tokens (Deprecated)" />

<Unstyled>

# System Old Space Tokens

## Deprecated Tokens

The following space tokens have been deprecated in v4. Space tokens depend on their context. `padding` tokens should be used for padding style property, `gap` should be used for gap or space between elements, and the `size` token should be used for height or width of an element. We recommend updating your code to use the new semantic token names directly.

<DeprecatedSpaceTokens />

</Unstyled>
