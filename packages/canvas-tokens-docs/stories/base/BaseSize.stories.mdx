import {Meta, Unstyled} from '@storybook/blocks';

import {BaseSizeTokens} from './examples/BaseSize';

<Meta title="Docs/Base Tokens/Size" />

<Unstyled>

## Base Size

Base size tokens provide the core sizes that make up our UI. They are used to build our system tokens.

> **Note:** Consumers should try to avoid using base tokens and instead use system tokens to future-proof theming.

<BaseSizeTokens />


</Unstyled>